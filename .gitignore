node_modules/
dist/
build/
coverage/
*.tsbuildinfo
*.local
.env
.env.*
!.env.example
logs/
*.log
.DS_Store
.vscode/*
!.vscode/extensions.json
frontend/dev-dist/

# Uploads de usuário (avatares etc.) — dado gerado em runtime, não faz parte do código.
backend/uploads/

# Skills/tooling do Claude Code instalados localmente (não são código do projeto)
.claude/
.agents/
skills-lock.json

# Backup/rollback de lotes de dados em produção — ficam só na máquina, não no repositório.
backend/scripts/*-produto-nao-lancar-*
