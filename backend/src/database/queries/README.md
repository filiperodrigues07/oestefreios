# Queries CHERP/Firebird

Reservado para as queries SQL reais do CHERP, fornecidas posteriormente (ver Fase 5 do roadmap).
Quando chegarem, cada query deve viver isolada aqui e ser usada exclusivamente pelos
repositories em `repositories/` (nunca diretamente por controllers/services).
